use usart trans data to PC, and use this software commproxy get data, then send to MQTT server.
so, this project can use minimum system to debug the MQTT without any network module such as wifi and 4G.

本例子使用stm32的串口发送MQTT消息到PC，然后在电脑上使用commproxy软件，将串口消息转发到MQTT服务器
这样可以用最小系统完成mqtt调试，不用网络模块
